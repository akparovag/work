#include <stdio.h>
#include <iostream>
#include <math.h>
#include <cmath>

using namespace std;

int main() //1
{
    long double x, y, k, z;

    cout << "Введите число: ";
    cin >> k;
    cin >> z;

    if (k < 1) 
        x = k* pow(z,3);
     else if (k >= 1)  
        x = z*(z+1);
    
    y = pow(log10(1 + pow(x,2)) + cos(x + 1),exp(k*x));
    
    cout <<"y="<<y<<"\n";
} 


int main() //2
{
    float x, y, a = 1.2, b = 2.3;

    cout << "Введите число: ";
    cin >> z;
    

    if (z < a*b) 
        x = sqrt(pow(a,2) + pow(b,2)*z);
     else if (z >= a*b)  
        x = pow(sin(z),2) + fabs(a*b*z);
    
    y = a*y + b*x*cos(sqrt(x)) / x + a*b;
    
    cout <<"y="<<y<<"\n";
}

int main() //3
{
    float x, y, pi=3.14, b = 2.3, z;

    cout << "Введите число: ";
    cin >> z;
    

    if (z < 1) 
        x = z/b;
     else if (z >= 1)  
        x = sqrt(pow((z*b),3));
    
    y = -pi + pow(cos(pow(x,3)),2) + pow(sin(pow(x,2)),3);
    
    cout <<"y="<<y<<"\n";
}

int main() //4
{
    float x, y, pi=3.14, z;

    cout << "Введите число: ";
    cin >> z;
    

    if (z < 1) 
        x = pow(z,3) + 0.3;
     else if (z >= 1)  
        x = z + log(z);
    
    y = pow(cos(pow(x,2)),3) + pow(sin(pow(x,3)),2);
    
    cout <<"y="<<y<<"\n";
}

int main() //5
{
    float x, y, pi=3.14, z;

    cout << "Введите число: ";
    cin >> z;
    

    if (z < -1) 
        x = -z/3;
     else if (z >= -1)  
        x = fabs(z);
    
    y = log(x + 0.5) + (exp(x) - exp(-x));
    
    cout <<"y="<<y<<"\n";
}

int main() //6 bad
{
    float x, y, z;

    cout << "Введите число: ";
    cin >> z;

    if (z < 0) 
        x = z;
    else if (z >= 0)  
        x = sin(z);
    
    y = 2/3*pow(sin(x),2) - 3/4*pow(cos(x),2);
    
    cout <<"y="<<y<<"\n";
} 

int main() //7
{
    float x, y, c=2, k=3, d, z;

    cout << "Введите число: ";
    cin >> z;

    if (z < 0) 
        x = pow(z,2) - z;
    else if (z >= 0)  
        x = pow(z,3);
    
    y = pow(sin(c*x + pow(d,2) + k*pow(x,2)),3);
    
    cout <<"y="<<y<<"\n";
}

int main() //8
{
    float x, y, z;

    cout << "Введите число: ";
    cin >> z;

    if (z >= 0) 
        x = 2*z + 1;
    else if (z < 0)  
        x = log(pow(z,2) - z);
    
    y = pow(sin(x) , 2) + pow(cos(pow(x,3)) , 5) + log(pow(x , 2/5));
    
    cout <<"y="<<y<<"\n";
} 

int main() //9
{
    float x, y, b, z;

    cout << "Введите число: ";
    cin >> z;

    if (z <= 0) 
        x = pow(z,b) + fabs(b/2);
    else if (z > 0)  
        x = sqrt(z);
    
    y = 1/cos(x) + log(fabs(tan(x/2)));
    
    cout <<"y="<<y<<"\n";
} 

int main() //10
{
    float x, y, b, z;

    cout << "Введите число: ";
    cin >> z;

    if (z >= 1) 
        x = z-1;
    else if (z < 1)  
        x = pow(z,2) + 1;
    
    y = pow(exp(x),pow(sin(x),2)) + log(x+1) / sqrt(x);
    
    cout <<"y="<<y<<"\n";
} 

int main() //11
{
    long double x, y, k, z;

    cout << "Введите число: ";
    cin >> z;

    if (z > 0) 
        x = 1 / pow(z,2) + 2*z;
     else if (z <= 0)  
        x = 1 - pow(z,3);
    
    y = pow(2*exp(x),-3*x) - 4 * pow(x,2) / log(fabs(x)) + x;
    
    cout <<"y="<<y<<"\n";
} 

int main() //12
{
    long double x, y, k, z;

    cout << "Введите число: ";
    cin >> z;

    if (z <= 0) 
        x = pow(z,2) + 5;
     else if (z > 0)  
        x = 1 / sqrt(z - 1);
    
    y = pow(sin(pow(x,2) - 1),3) + log(fabs(x)) + exp(x);
    
    cout <<"y="<<y<<"\n";
} 

int main() //13
{
    long double x, y, k = 1, m = 2, n = -2, z;

    cout << "Введите число: ";
    cin >> z;

    if (z > 1) 
        x = pow(exp(x),z) + z;
     else if (z <= 1)  
        x = pow(z,2) + 1;
    
    y = sin(n*x) + cos(k*x) + log(m*x);
    
    cout <<"y="<<y<<"\n";
} 

int main() //14
{
    long double x, y, z, n = -1, k = 3, m = 2;
    
    cout << "Введите число:";
    cin >> z;
    
    if (z > 0) 
        x = sqrt(z);
    else if (z <= 0)   
        x = (3*pow(z,3)) - 5;
  
    y = cos(5*x) + sin(1/5 * x) + exp(x);    
    
    cout <<"y="<<y<<"\n";
}
